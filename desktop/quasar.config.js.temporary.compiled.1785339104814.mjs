/* eslint-disable */
/**
 * THIS FILE IS GENERATED AUTOMATICALLY.
 * 1. DO NOT edit this file directly as it won't do anything.
 * 2. EDIT the original quasar.config file INSTEAD.
 * 3. DO NOT git commit this file. It should be ignored.
 *
 * This file is still here because there was an error in
 * the original quasar.config file and this allows you to
 * investigate the Node.js stack error.
 *
 * After you fix the original file, this file will be
 * deleted automatically.
 **/


// quasar.config.js
import { defineConfig } from "@quasar/app-vite/wrappers";
import { fileURLToPath } from "node:url";
var __quasar_inject_import_meta_url__ = "file:///C:/Users/sorok/Music/yamp3/desktop/quasar.config.js";
var quasar_config_default = defineConfig(() => {
  return {
    boot: ["auth"],
    css: ["app.scss"],
    extras: ["roboto-font", "material-icons"],
    build: {
      target: {
        browser: ["es2022", "edge111", "firefox115", "chrome115", "safari15"],
        node: "node20"
      },
      typescript: {
        strict: true,
        vueShim: true
      },
      vueRouterMode: "hash",
      // hash — чтобы навигация работала внутри webview Tauri
      distDir: "dist/spa",
      // Алиас @ -> src (Quasar не добавляет его по умолчанию; нужен для import "@/...")
      alias: {
        "@": fileURLToPath(new URL("./src", __quasar_inject_import_meta_url__))
      }
    },
    devServer: {
      port: 9e3,
      open: false
    },
    framework: {
      config: {
        dark: true
        // тёмная тема по умолчанию (Spotify-like)
      },
      plugins: ["Notify", "Loading"]
    },
    animations: [],
    ssr: false
  };
});
export {
  quasar_config_default as default
};
